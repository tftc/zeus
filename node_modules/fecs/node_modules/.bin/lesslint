#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../lesslint/bin/lesslint-cli" "$@"
  ret=$?
else 
  node  "$basedir/../lesslint/bin/lesslint-cli" "$@"
  ret=$?
fi
exit $ret
