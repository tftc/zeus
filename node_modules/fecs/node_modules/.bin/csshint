#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../csshint/bin/csshint-cli" "$@"
  ret=$?
else 
  node  "$basedir/../csshint/bin/csshint-cli" "$@"
  ret=$?
fi
exit $ret
