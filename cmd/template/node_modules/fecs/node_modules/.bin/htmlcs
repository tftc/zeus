#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../htmlcs/bin/htmlcs" "$@"
  ret=$?
else 
  node  "$basedir/../htmlcs/bin/htmlcs" "$@"
  ret=$?
fi
exit $ret
